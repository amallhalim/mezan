branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "🚫 ERROR: You are trying to rebase the '$branch' branch."
  echo "Rebasing main/master is dangerous and has been blocked by Husky."
  exit 1
fi
